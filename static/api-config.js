/* ============================================================
   API CONFIG — the ONLY file you must edit after deploying.

   Set API_BASE to your Railway backend URL (no trailing slash).
   Example:  window.API_BASE = 'https://technova-backend-production.up.railway.app';

   For local development keep localhost.
   ============================================================ */
window.API_BASE = 'http://tech-nova-backend-production.up.railway.app';
